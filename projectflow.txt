1. Create project template by executing template.py file
2. Write a code on setup.py and pyproject.toml file to import local packages
   - find more about "setup.py and pyproject.toml" at crashcourse.txt
3. Create a virtual env , activate it and install the requirements from requirements.txt
    conda create -n proj1 python=3.18.3 -y
    conda activate proj1
    do pip install -r requirements.txt
    add required modules to requirements.txt
   
4. Do a "pip list" on terminal to make sure you have install all your local packages


-------------------------------------------------Mongodb setup-----------------------------------------------------------------------------
5. Sign up to mongodb atlas and create a new project by just providing it a name than next next create.
6. from "create a cluster" screen , hit "create ", Select M0 servicekeeping other service as default , hit
7. Setup the username and password then create a DB user
8. Go to "network access" and add ip address - "0.0.0.0/0" so that we can acccess it from anywhere
9. Go back to project >> "get connection String ">>"Drivers">>{Driver:Python ,Version: 3.6 or later}
   - copy and save the connection string with you (replce password). -Done
10. Create folder "notebook" >>do step 11>>create file "mongoDB_demo.ipynb">> select kernal>>python kernel>visa>>
11. Dataset added to notebook folder
12. Push your data to mongodb database from your python motebook
13. Go to mongodb atlas>> Database >> browsw collection>> see your data in key value format


-----------------------------------------logging,exception and notebooks------------------------------------------------------------------------------------
14. Write a logger file and test it in your demo.py
15. Write the exception file and test in your demo.py
16. EDA and feature engg notebook added               
---------------------------------------------Data Ingestion----------------------------------------------------------------------------
17. Before we work on "Data Ingestion" component >> Declare variables within constants. __init.py file>>
    add code to configuration/mongo_db.connections.py file and define the func for mondodb connection>>
    Inside "data_access" folder ,add code to  proj1_data that will use mongo_db_connections.py
    to connect with db , fetch tha data in key value format and transform that to df>>
    add code to entity.config_entity.py file till dataingestionconfig class>>
    add code to entity.artifact_entity.py file till dataingestionartifact class>>
    add code to components.data_ingestion.py file>>add code to training pipeline>>
    run demo.py (set mongodb connection url first , see next step)
18. To setup connection url for windows,open bash/powershell terminal and run below commands:
      *** For Bash ***
    set: export MONGODB_URL="mongodb+srv://<username>:<password>......"
    check: echo $MONGODB_URL
                        *** For Powershell ***
    set: $env:MONGODB_URL = "mongodb+srv://<username>:<password>......"
    check: echo $env:MONGODB_URL

    To setup the connection url on Windows, open env variable setting option and add a new variable:
    Name: MONGODB_URL, Value = <url>
    Also add "artifact" dir to .gitignore file

---------------------------------------------------------------DATA VALIDATION,DATA TANSFORMATION & MODEL TRAINER--------------------------------------------------
19. Complete the work on utils.main_utils.py and config.schema.yaml file(add enteries info about dataset for validation step)
20. Now works on data validation components the way we did in setp 17 for data Ingestion(Workflow mentioned below)
21. Now work on data transformation component the way we did in above step (add estimator.py to entity folder)
22. Now work on the model trainer compnent the way we did in above step (add class to estimator.py in entity folder)



